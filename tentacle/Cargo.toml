[package]
name = "tentacle"
version = "0.7.5"
license = "MIT"
description = "Minimal implementation for a multiplexed p2p network framework."
authors = ["piaoliu <driftluo@foxmail.com>", "Nervos Core Dev <dev@nervos.org>"]
repository = "https://github.com/nervosnetwork/tentacle"
readme = "README.md"
keywords = ["network", "peer-to-peer"]
categories = ["network-programming", "asynchronous"]
edition = "2024"
rust-version = "1.88.0"

[package.metadata.docs.rs]
features = ["tokio-runtime", "tokio-timer", "upnp", "ws", "unstable", "tls"]
all-features = false
no-default-features = true
rustc-args = ["--cfg", "docsrs"]

[dependencies]
yamux = { path = "../yamux", version = "0.3.14", default-features = false, package = "tokio-yamux" }
secio = { path = "../secio", version = "0.6.6", package = "tentacle-secio" }

futures = { version = "0.3.0" }
tokio = { version = "1.0.0", features = ["macros"] }
tokio-util = { version = "0.7.0", features = ["codec"] }
async-trait = "0.1"
log = "0.4"
bytes = "1.0.0"
thiserror = "1.0"
nohash-hasher = "0.2"

parking_lot = { version = "0.12", optional = true }
tokio-tungstenite = { version = "0.28", optional = true }
httparse = { version = "1.9", optional = true }
futures-timer = { version = "3.0.2", optional = true }

multiaddr = { path = "../multiaddr", package = "tentacle-multiaddr", version = "0.3.6" }
url = "2.5.4"
molecule = "0.9.0"

# upnp
igd = { version = "0.16", optional = true, package = "igd-next" }

#tls
tokio-rustls = { version = "0.26.0", optional = true }

[target.'cfg(not(target_family = "wasm"))'.dependencies]
# rand 0.8 not support wasm32
rand = "0.8"
socket2 = { version = "0.6.0", features = ["all"] }
fast-socks5 = "0.10.0"
quinn = { version = "0.11", optional = true, default-features = false, features = [
    "runtime-tokio", "rustls-aws-lc-rs"
] }
rustls = { version = "0.23", optional = true, default-features = false, features = [
    "aws_lc_rs", "std"
] }
rcgen = { version = "0.13", optional = true, default-features = false, features = [
    "pem", "aws_lc_rs"
] }
x509-parser = { version = "0.16", optional = true }
ring = { version = "0.17", optional = true }

[target.'cfg(target_family = "wasm")'.dependencies]
js-sys = "0.3"
wasm-bindgen = "0.2"
wasm-bindgen-futures = "0.4"

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[target.'cfg(windows)'.dependencies]
winapi = { version = "0.3.7", features = [
    "minwindef",
    "ws2def",
    "winerror",
    "heapapi",
] }

[dev-dependencies]
env_logger = "0.6.0"
crossbeam-channel = "0.5"
systemstat = "0.2"
futures-test = "0.3.5"

[target.'cfg(unix)'.dev-dependencies]
nix = { version = "0.29", default-features = false, features = ["signal"] }

[[example]]
name = "simple_using_spawn"
required-features = ["unstable"]

[[example]]
name = "quic_simple"
required-features = ["quic"]

[[example]]
name = "quic_qa_error_matrix"
required-features = ["quic"]

[[example]]
name = "quic_qa_coexist"
required-features = ["quic"]

[[example]]
name = "quic_qa_idle_timeout"
required-features = ["quic"]

[[example]]
name = "quic_qa_listener_survives_bad_handshake"
required-features = ["quic"]

[[example]]
name = "quic_qa_duplicate_dial"
required-features = ["quic"]

[[example]]
name = "quic_qa_peer_id_dedup"
required-features = ["quic"]

[[example]]
name = "quic_qa_keep_alive"
required-features = ["quic"]

[[test]]
name = "test_quic_verifier"
required-features = ["quic"]

[features]
default = ["tokio-runtime", "tokio-timer", "quic"]
ws = ["tokio-tungstenite", "httparse"]
tls = ["tokio-rustls"]
upnp = ["igd"]
secio-async = ["secio/async-sign"]
unstable = []
metrics = ["yamux/metrics"]

openssl-vendored = ["secio/openssl-vendored"]

# Related to runtime

tokio-timer = ["yamux/tokio-timer", "tokio/time", "tokio-runtime"]
tokio-runtime = ["tokio/io-util", "tokio/net", "tokio/rt-multi-thread"]

generic-timer = ["futures-timer", "yamux/generic-timer"]
wasm-timer = ["futures-timer", "yamux/wasm", "futures-timer/wasm-bindgen"]
quic = ["dep:quinn", "dep:rustls", "dep:rcgen", "dep:x509-parser", "dep:ring"]
