//! Stub kept so `lib.rs` can re-export it as the shared protocol surface in
//! future iterations. Empty for now — each binary defines its own protocol
//! locally because their needs (gossip / ping / counting) diverge enough that
//! a shared abstraction would obscure more than it helps.
